
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local player = Players.LocalPlayer
local playerGui = player.PlayerGui

local isMainGame = game.PlaceId == 84988808589910
local isRaid = game.PlaceId == 96105075537655

if not isMainGame and not isRaid then
    error("This script does not work in this game!")
end

-- Anti AFK
local afkGui = Instance.new("ScreenGui")
afkGui.Name = "AntiAFK"
afkGui.ResetOnSpawn = false
afkGui.Parent = playerGui

local afkFrame = Instance.new("Frame")
afkFrame.Size = UDim2.new(0, 370, 0, 160)
afkFrame.Position = UDim2.new(0.7, 0, 0.1, 0)
afkFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
afkFrame.BorderSizePixel = 0
afkFrame.Active = true
afkFrame.Draggable = true
afkFrame.Parent = afkGui

local afkCorner = Instance.new("UICorner")
afkCorner.CornerRadius = UDim.new(0, 10)
afkCorner.Parent = afkFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 50)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
titleLabel.BorderSizePixel = 0
titleLabel.Text = "Anti AFK"
titleLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
titleLabel.TextSize = 22
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Parent = afkFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 10)
titleCorner.Parent = titleLabel

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 60)
statusLabel.Position = UDim2.new(0, 0, 0, 55)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "Status: Active"
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
statusLabel.TextSize = 20
statusLabel.Font = Enum.Font.GothamSemibold
statusLabel.Parent = afkFrame

local creditLabel = Instance.new("TextLabel")
creditLabel.Size = UDim2.new(1, 0, 0, 30)
creditLabel.Position = UDim2.new(0, 0, 1, -30)
creditLabel.BackgroundTransparency = 1
creditLabel.Text = "Anti AFK - Updated"
creditLabel.TextColor3 = Color3.fromRGB(100, 100, 100)
creditLabel.TextSize = 14
creditLabel.Font = Enum.Font.Gotham
creditLabel.Parent = afkFrame

player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
    statusLabel.Text = "Roblox tried to kick you!\nBlocked it successfully!"
    statusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
    task.wait(3)
    statusLabel.Text = "Status: Active"
    statusLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
end)

-- Menu UI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MobileUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = game:GetService("CoreGui")

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 40)
mainFrame.Position = UDim2.new(0.5, -100, 0, 10)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 10)
mainCorner.Parent = mainFrame

local dragging = false
local dragStart, startPos

mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

mainFrame.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

mainFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

local titleBtn = Instance.new("TextButton")
titleBtn.Size = UDim2.new(1, 0, 0, 40)
titleBtn.BackgroundTransparency = 1
titleBtn.Text = "⚙ Menu  ▼"
titleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
titleBtn.TextSize = 15
titleBtn.Font = Enum.Font.GothamBold
titleBtn.Parent = mainFrame

local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, 0, 0, 0)
contentFrame.Position = UDim2.new(0, 0, 0, 40)
contentFrame.BackgroundTransparency = 1
contentFrame.ClipsDescendants = true
contentFrame.Parent = mainFrame

local UILayout = Instance.new("UIListLayout")
UILayout.SortOrder = Enum.SortOrder.LayoutOrder
UILayout.Padding = UDim.new(0, 4)
UILayout.Parent = contentFrame

local tabFrame = Instance.new("Frame")
tabFrame.Size = UDim2.new(1, 0, 0, 30)
tabFrame.BackgroundTransparency = 1
tabFrame.LayoutOrder = 0
tabFrame.Parent = contentFrame

local tabLayout = Instance.new("UIListLayout")
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.Padding = UDim.new(0, 4)
tabLayout.Parent = tabFrame

local function makeTabBtn(text, color, order)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.48, 0, 1, 0)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    btn.LayoutOrder = order
    btn.Parent = tabFrame
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 6)
    c.Parent = btn
    return btn
end

local mainTabBtn
local combatTabBtn

if isMainGame then
    mainTabBtn = makeTabBtn("Main", Color3.fromRGB(70, 130, 220), 1)
    combatTabBtn = makeTabBtn("Combat", Color3.fromRGB(50, 50, 50), 2)
else
    combatTabBtn = makeTabBtn("Combat", Color3.fromRGB(150, 70, 220), 1)
end

local mainContainer = Instance.new("Frame")
mainContainer.Size = UDim2.new(1, 0, 0, 0)
mainContainer.BackgroundTransparency = 1
mainContainer.LayoutOrder = 1
mainContainer.Visible = isMainGame
mainContainer.Parent = contentFrame

local mainList = Instance.new("UIListLayout")
mainList.Padding = UDim.new(0, 4)
mainList.Parent = mainContainer

local combatContainer = Instance.new("Frame")
combatContainer.Size = UDim2.new(1, 0, 0, 0)
combatContainer.BackgroundTransparency = 1
combatContainer.LayoutOrder = 1
combatContainer.Visible = isRaid
combatContainer.Parent = contentFrame

local combatList = Instance.new("UIListLayout")
combatList.Padding = UDim.new(0, 4)
combatList.Parent = combatContainer

local function makeToggle(parent, labelText, startState, onToggle)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -8, 0, 32)
    row.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    row.BorderSizePixel = 0
    row.Parent = parent

    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 6)
    rowCorner.Parent = row

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 12
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = row

    local togBtn = Instance.new("TextButton")
    togBtn.Size = UDim2.new(0, 44, 0, 22)
    togBtn.Position = UDim2.new(1, -52, 0.5, -11)
    togBtn.BackgroundColor3 = startState and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(80, 80, 80)
    togBtn.Text = startState and "ON" or "OFF"
    togBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    togBtn.TextSize = 11
    togBtn.Font = Enum.Font.GothamBold
    togBtn.BorderSizePixel = 0
    togBtn.Parent = row

    local togCorner = Instance.new("UICorner")
    togCorner.CornerRadius = UDim.new(0, 11)
    togCorner.Parent = togBtn

    local state = startState
    togBtn.MouseButton1Click:Connect(function()
        state = not state
        togBtn.Text = state and "ON" or "OFF"
        togBtn.BackgroundColor3 = state and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(80, 80, 80)
        onToggle(state)
    end)

    return row
end

local function updateSize(container, listLayout)
    container.Size = UDim2.new(1, 0, 0, listLayout.AbsoluteContentSize.Y + 4)
end

if isMainGame then
    makeToggle(mainContainer, "Boss Spawn", false, function(value)
        pcall(function()
            local button = playerGui.Button["Boss Spawn"]
            button.Visible = value
            button.Position = UDim2.new(0.800000012, 1, 9.99999972e-10, 0)
            button.Size = UDim2.new(0.200000003, 0, 0.300000012, 10)
        end)
    end)

    makeToggle(mainContainer, "Night Market", false, function(value)
        pcall(function()
            local button = playerGui.Button["Night Market"]
            button.Visible = value
            button.Position = UDim2.new(0.400000006, 1, 0, 0)
            button.Size = UDim2.new(0.100000001, 20, 0.200000003, 25)
        end)
    end)

    makeToggle(mainContainer, "Teleporter", false, function(value)
        pcall(function()
            local button = playerGui.Button.Teleport
            button.Visible = value
            button.Position = UDim2.new(0.800000012, 1, 9.99999972e-10, 0)
            button.Size = UDim2.new(0.200000003, 0, 0.300000012, 10)
        end)
    end)

    makeToggle(mainContainer, "Shop", false, function(value)
        pcall(function()
            local button = playerGui.Button["Shop Item"]
            button.Visible = value
            button.Position = UDim2.new(0.600000024, 1, 0.0199999996, 0)
            button.Size = UDim2.new(0.100000001, 20, 0.200000003, 25)
        end)
    end)

    makeToggle(mainContainer, "Chest Exchange", false, function(value)
        pcall(function()
            playerGui.Button["Chest Exchange"].Visible = value
        end)
    end)

    makeToggle(mainContainer, "Craft", false, function(value)
        pcall(function()
            playerGui.Button.Craft.Visible = value
        end)
    end)

    mainList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        updateSize(mainContainer, mainList)
    end)
    updateSize(mainContainer, mainList)
end

local hakiEnabled = false
local observationEnabled = false
local swordEnabled = false
local hakariEnabled = false
local dragonSlayerEnabled = false
local autoM1Enabled = false

makeToggle(combatContainer, "Auto Haki", false, function(value)
    hakiEnabled = value
end)

makeToggle(combatContainer, "Auto Observation Haki", false, function(value)
    observationEnabled = value
end)

makeToggle(combatContainer, "Auto Sword Skill", false, function(value)
    swordEnabled = value
end)

makeToggle(combatContainer, "Auto Hakari Skill", false, function(value)
    hakariEnabled = value
end)

makeToggle(combatContainer, "Auto Dragon Slayer Skill", false, function(value)
    dragonSlayerEnabled = value
end)

makeToggle(combatContainer, "Auto M1", false, function(value)
    autoM1Enabled = value
end)

combatList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    updateSize(combatContainer, combatList)
end)
updateSize(combatContainer, combatList)

local activeTab = isRaid and "combat" or "main"

if isMainGame then
    mainTabBtn.MouseButton1Click:Connect(function()
        activeTab = "main"
        mainContainer.Visible = true
        combatContainer.Visible = false
        mainTabBtn.BackgroundColor3 = Color3.fromRGB(70, 130, 220)
        combatTabBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end)

    combatTabBtn.MouseButton1Click:Connect(function()
        activeTab = "combat"
        mainContainer.Visible = false
        combatContainer.Visible = true
        combatTabBtn.BackgroundColor3 = Color3.fromRGB(150, 70, 220)
        mainTabBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end)
end

local expanded = false
titleBtn.MouseButton1Click:Connect(function()
    expanded = not expanded
    titleBtn.Text = expanded and "⚙ Menu  ▲" or "⚙ Menu  ▼"
    if expanded then
        local totalHeight = 40 + 30 + 4 + (activeTab == "main" and mainList.AbsoluteContentSize.Y or combatList.AbsoluteContentSize.Y) + 12
        mainFrame.Size = UDim2.new(0, 200, 0, totalHeight)
        contentFrame.Size = UDim2.new(1, 0, 0, totalHeight - 40)
    else
        mainFrame.Size = UDim2.new(0, 200, 0, 40)
        contentFrame.Size = UDim2.new(1, 0, 0, 0)
    end
end)

task.spawn(function()
    while task.wait() do
        if hakiEnabled then
            pcall(function()
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside"):FireServer("Server", "Misc", "Haki", 1)
            end)
        end
    end
end)

player.CharacterAdded:Connect(function()
    if hakiEnabled then
        task.wait(1)
        pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside"):FireServer("Server", "Misc", "Haki", 1)
        end)
    end
end)

task.spawn(function()
    while task.wait() do
        if observationEnabled then
            pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Misc", "Observation", 1)
                remote:FireServer("Server", "Misc", "Observation", 1)
                remote:FireServer("Server", "Misc", "Observation", 1)
                remote:FireServer("Server", "Misc", "Observation", 1)
            end)
        end
    end
end)

task.spawn(function()
    while task.wait() do
        if swordEnabled then
            pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Sword", "Skill1", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill2", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill3", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill1", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill2", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill3", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill4", "Demon Lord's Dual Dagger")
            end)
        end
    end
end)

task.spawn(function()
    while task.wait() do
        if hakariEnabled then
            pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Combat", "Skill1", "Hakari")
                remote:FireServer("Server", "Combat", "Skill2", "Hakari")
                remote:FireServer("Server", "Combat", "Skill3", "Hakari")
            end)
        end
    end
end)

task.spawn(function()
    while task.wait() do
        if dragonSlayerEnabled then
            pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Sword", "Skill1", "Dragon Slayer")
                remote:FireServer("Server", "Sword", "Skill2", "Dragon Slayer")
                remote:FireServer("Server", "Sword", "Skill3", "Dragon Slayer")
                remote:FireServer("Server", "Sword", "Skill4", "Dragon Slayer")
                remote:FireServer("Server", "Sword", "Skill5", "Dragon Slayer")
            end)
        end
    end
end)

task.spawn(function()
    while task.wait() do
        if autoM1Enabled then
            pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Sword", "M1s", "Demon Lord's Dual Dagger", 1)
            end)
        end
    end
end)

print("UI loaded successfully!")
