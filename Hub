local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local player = Players.LocalPlayer
local TweenService = game:GetService("TweenService")

-- Game specific scripts
loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/antiafk"))()

if game.PlaceId == 84988808589910 or game.PlaceId == 96105075537655 then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/ROGUEPIECE"))()
end

if game.PlaceId == 77747658251236 or game.PlaceId == 99684056491472 or game.PlaceId == 75159314259063 or game.PlaceId == 96767841099256 then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/SAILOR%20PIECE"))()
end

if game.PlaceId == 79305036070450 then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/spinabaddie"))()
end

-- Main Hub UI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MainHub"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 180)
frame.Position = UDim2.new(0.5, -110, 0, 10)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(60, 60, 80)
stroke.Thickness = 1.5
stroke.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -40, 0, 35)
title.Position = UDim2.new(0, 10, 0, 5)
title.BackgroundTransparency = 1
title.Text = "🎮 MAIN HUB"
title.TextColor3 = Color3.fromRGB(220, 220, 255)
title.TextSize = 14
title.Font = Enum.Font.GothamBold
title.Parent = frame

-- X button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 26, 0, 26)
closeBtn.Position = UDim2.new(1, -32, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 12
closeBtn.Font = Enum.Font.GothamBold
closeBtn.BorderSizePixel = 0
closeBtn.Parent = frame

local closeBtnCorner = Instance.new("UICorner")
closeBtnCorner.CornerRadius = UDim.new(0, 6)
closeBtnCorner.Parent = closeBtn

-- Reopen circle button
local miniCircle = Instance.new("TextButton")
miniCircle.Size = UDim2.new(0, 50, 0, 50)
miniCircle.Position = UDim2.new(0.5, -25, 0, 10)
miniCircle.BackgroundColor3 = Color3.fromRGB(80, 80, 200)
miniCircle.Text = "🎮"
miniCircle.TextSize = 20
miniCircle.Font = Enum.Font.GothamBold
miniCircle.BorderSizePixel = 0
miniCircle.Visible = false
miniCircle.Active = true
miniCircle.Draggable = true
miniCircle.Parent = screenGui

local miniCorner = Instance.new("UICorner")
miniCorner.CornerRadius = UDim.new(1, 0)
miniCorner.Parent = miniCircle

closeBtn.MouseButton1Click:Connect(function()
    frame.Visible = false
    miniCircle.Visible = true
end)

miniCircle.MouseButton1Click:Connect(function()
    frame.Visible = true
    miniCircle.Visible = false
end)

-- Toggle maker
local function makeToggle(labelText, yPos, onEnable, onDisable)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -16, 0, 32)
    row.Position = UDim2.new(0, 8, 0, yPos)
    row.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    row.BorderSizePixel = 0
    row.Parent = frame

    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 8)
    rowCorner.Parent = row

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.65, 0, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 12
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = row

    local togBtn = Instance.new("TextButton")
    togBtn.Size = UDim2.new(0, 50, 0, 24)
    togBtn.Position = UDim2.new(1, -58, 0.5, -12)
    togBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    togBtn.Text = "OFF"
    togBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    togBtn.TextSize = 11
    togBtn.Font = Enum.Font.GothamBold
    togBtn.BorderSizePixel = 0
    togBtn.Parent = row

    local togCorner = Instance.new("UICorner")
    togCorner.CornerRadius = UDim.new(0, 12)
    togCorner.Parent = togBtn

    local state = false
    togBtn.MouseButton1Click:Connect(function()
        state = not state
        if state then
            togBtn.Text = "ON"
            togBtn.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
            onEnable()
        else
            togBtn.Text = "OFF"
            togBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
            onDisable()
        end
    end)
end

-- Kill Aura toggle
local killAuraGui = nil
makeToggle("⚔ Kill Aura", 45, function()
    if killAuraGui then killAuraGui:Destroy() end
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/killauraonly"))()
    killAuraGui = player.PlayerGui:FindFirstChild("KillAura")
end, function()
    if killAuraGui then
        killAuraGui:Destroy()
        killAuraGui = nil
    end
end)

-- Auto Clicker toggle
local autoClickerGui = nil
makeToggle("🖱 Auto Clicker", 85, function()
    if autoClickerGui then autoClickerGui:Destroy() end
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Game-Roblox-ops/SB-NM/refs/heads/main/ACV3%3F"))()
    autoClickerGui = player.PlayerGui:FindFirstChild("AutoClicker")
end, function()
    if autoClickerGui then
        autoClickerGui:Destroy()
        autoClickerGui = nil
    end
end)

print("Hub loaded!")
