local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player.PlayerGui

local buttons = {"Boss Spawn", "Night Market", "Teleporter", "Shop", "Chest Exchange"}
for _, name in ipairs(buttons) do
    local ok, e = pcall(function()
        playerGui.Button[name].Visible = false
    end)
    if not ok then warn(name .. " not found: " .. e) end
end

local Window = Rayfield:CreateWindow({
    Name = "Menu",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "Please wait",
    ConfigurationSaving = {
        Enabled = false,
    },
    KeySystem = false,
})

local Tab = Window:CreateTab("Main", 4483362458)
local CombatTab = Window:CreateTab("Combat", 4483362458)

Tab:CreateToggle({
    Name = "Boss Spawn UI",
    CurrentValue = false,
    Flag = "BossSpawnToggle",
    Callback = function(value)
        local ok, e = pcall(function()
            playerGui.Button["Boss Spawn"].Visible = value
        end)
        if not ok then warn("Boss Spawn not found: " .. e) end
    end,
})

Tab:CreateToggle({
    Name = "Night Market UI",
    CurrentValue = false,
    Flag = "NightMarketToggle",
    Callback = function(value)
        local ok, e = pcall(function()
            playerGui.Button["Night Market"].Visible = value
        end)
        if not ok then warn("Night Market not found: " .. e) end
    end,
})

Tab:CreateToggle({
    Name = "Teleporter UI",
    CurrentValue = false,
    Flag = "TeleporterToggle",
    Callback = function(value)
        local ok, e = pcall(function()
            playerGui.Button["Teleporter"].Visible = value
        end)
        if not ok then warn("Teleporter not found: " .. e) end
    end,
})

Tab:CreateToggle({
    Name = "Shop UI",
    CurrentValue = false,
    Flag = "ShopToggle",
    Callback = function(value)
        local ok, e = pcall(function()
            playerGui.Button["Shop"].Visible = value
        end)
        if not ok then warn("Shop not found: " .. e) end
    end,
})

Tab:CreateToggle({
    Name = "Chest Exchange UI",
    CurrentValue = false,
    Flag = "ChestExchangeToggle",
    Callback = function(value)
        local ok, e = pcall(function()
            playerGui.Button["Chest Exchange"].Visible = value
        end)
        if not ok then warn("Chest Exchange not found: " .. e) end
    end,
})

local hakiEnabled = false
local observationEnabled = false
local swordEnabled = false

CombatTab:CreateToggle({
    Name = "Auto Haki",
    CurrentValue = false,
    Flag = "AutoHakiToggle",
    Callback = function(value)
        hakiEnabled = value
    end,
})

CombatTab:CreateToggle({
    Name = "Auto Observation Haki",
    CurrentValue = false,
    Flag = "ObservationToggle",
    Callback = function(value)
        observationEnabled = value
    end,
})

CombatTab:CreateToggle({
    Name = "Auto Sword Skill",
    CurrentValue = false,
    Flag = "SwordToggle",
    Callback = function(value)
        swordEnabled = value
    end,
})

task.spawn(function()
    while task.wait() do
        if hakiEnabled then
            local ok, e = pcall(function()
                local args = {"Server", "Misc", "Haki", 1}
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside"):FireServer(unpack(args))
            end)
            if not ok then warn("Auto Haki error: " .. e) end
        end
    end
end)

player.CharacterAdded:Connect(function()
    if hakiEnabled then
        task.wait(1)
        local ok, e = pcall(function()
            local args = {"Server", "Misc", "Haki", 1}
            game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside"):FireServer(unpack(args))
        end)
        if not ok then warn("Spawn Haki error: " .. e) end
    end
end)

task.spawn(function()
    while task.wait() do
        if observationEnabled then
            local ok, e = pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                local args = {"Server", "Misc", "Observation", 1}
                remote:FireServer(unpack(args))
                remote:FireServer(unpack(args))
                remote:FireServer(unpack(args))
                remote:FireServer(unpack(args))
            end)
            if not ok then warn("Observation Haki error: " .. e) end
        end
    end
end)

task.spawn(function()
    while task.wait() do
        if swordEnabled then
            local ok, e = pcall(function()
                local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Serverside")
                remote:FireServer("Server", "Sword", "Skill1", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill2", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill3", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill", "Inverted Spear of Heaven")
                remote:FireServer("Server", "Sword", "Skill1", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill2", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill3", "Demon Lord's Dual Dagger")
                remote:FireServer("Server", "Sword", "Skill4", "Demon Lord's Dual Dagger")
            end)
            if not ok then warn("Sword Skill error: " .. e) end
        end
    end
end)

print("UI loaded successfully!")
